# System / Global / Local
```bash
git confi --{system, global, local}
	//{all users, all repositories, currente repository}
```
# User
```bash
git config --global user.name "<user_name>"
git config --global user.email "<user_email>"
```
# End of Line
```bash
git config --global core.autocrlf true  //If in windows
								  input //If on linux/mac
```