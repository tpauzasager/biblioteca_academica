```bash
git init 
git add .
git commit -m "fisrt commit"
<create a new repo>
git push -u origin master
```
# Create a remote repository
```bash
gh repo create
# Follow the steps (choose push an existing local repo and push the one created previously)
```