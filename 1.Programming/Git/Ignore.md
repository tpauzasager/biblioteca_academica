# Behaviour 
- It will not add the specific files into the staging area, but, if they are already there, the changes will be commited anyways
- To see how modify the staging area see [[Staging]]
```bash
//Prints the name of the file into .gitignore file
echo <files_to_ignore> > .gitignore 
```