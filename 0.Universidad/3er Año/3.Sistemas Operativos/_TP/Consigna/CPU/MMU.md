# Dirección física
$$\text{Dir física} = Base + desplazamiento$$
Se debe validar que las solicitudes estén dentro del límite de la partición asignada
Si falla la validación => Ocurre "Segmentation Fault", se guarda contexto en MEMORIA y devolver el TID al KERNEL junto con motivo de Segmentatio Fault