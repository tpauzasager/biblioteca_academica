# Contexto de ejecución 
Por cada PID:
	Almacenar Base y Límite
Por cada TID:
	Almacenar los registros del CPU (inicializados en cero)

Pseudocódigo:
	Por cada TID se lee su archivo de pseudocódigo y se guarda de forma estructurada las instrucciones (para ir devolviendolas al CPU a pedido)
