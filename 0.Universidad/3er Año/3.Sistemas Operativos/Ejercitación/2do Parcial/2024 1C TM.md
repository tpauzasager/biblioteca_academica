1. 
   a. Si, ya que la dirección lógica es propia de cada proceso, por lo que, si se tienen varios procesos ejecutando al mismo tiempo, se podría llenar toda la memoría con direcciones lógicas más pequeñes al tamaño de la misma.
   b. 
2. 
   Se podría analisar el estado de la CPU, es decir, si se encuentra realizando trabajo productivo o tambien se podría comparar la suma de conjuntos de trabajo con la sumatoria de frames disponibles en el sistema. Para solucionarlo, disminuiría el grado de multiprogramación para que así los procesos restantes tengan acceso a una mayor cantidad de frames. 
3. 
   Ya se hizo
4. 
   Ya se hizo
5. 
   a.
   b. Falso. Gracias a que la FAT se encuentra cargada en memoría, solo es necesario iterar por sus entradas para encontrar el puntero al último bloque de un archivo. 