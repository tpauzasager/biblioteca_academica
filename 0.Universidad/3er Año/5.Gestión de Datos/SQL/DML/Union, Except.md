```sql
SELECT ..
	FROM
	WHERE
[UNION,EXCEPT]
SELECT
	FROM
	WHERE
ORDER BY ...
```

Union devuelve filas de ambas consultas (las dos tiene que tener las mismas columnas)

Except devuelve filas de la 1ra consulta que no aparezcan en la 2da
